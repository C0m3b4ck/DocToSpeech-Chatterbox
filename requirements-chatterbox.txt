# ========================= CHATTERBOX (separate venv) =========================
# Why separate: pins torch==2.6.0 + transformers==5.2.0, conflicts with coqui-tts
#
# Setup:
#   python -m venv .venv-chatterbox
#   source .venv-chatterbox/bin/activate
#
#   Step 1: Install torch first (resolves CUDA wheels from PyPI):
#     pip install torch==2.6.0 torchaudio==2.6.0
#
#   Step 2: Install chatterbox without pulling its own deps (already installed):
#     pip install chatterbox-tts --no-deps
#
#   Step 3: Install remaining deps:
#     pip install numpy librosa s3tokenizer transformers==5.2.0 diffusers==0.29.0
#     pip install resemble-perth conformer==0.3.2 safetensors==0.5.3
#     pip install spacy-pkuseg pykakasi==2.3.0 pyloudnorm omegaconf
#
#   Step 4: Install doc parsing tools:
#     pip install epub2txt docx2txt pdftotext html2text pyfiglet tqdm rich ollama
#
#   Then run doctospeech from this venv.

# --- This file is a reference only. Follow the steps above. ---

# torch==2.6.0
# torchaudio==2.6.0
# chatterbox-tts
# numpy
# librosa
# s3tokenizer
# transformers==5.2.0
# diffusers==0.29.0
# resemble-perth
# conformer==0.3.2
# safetensors==0.5.3
# spacy-pkuseg
# pykakasi==2.3.0
# pyloudnorm
# omegaconf
# epub2txt
# docx2txt
# pdftotext
# html2text
# pyfiglet
# tqdm
# rich
# ollama
