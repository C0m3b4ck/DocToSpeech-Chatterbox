# ========================= SESAME CSM (separate venv) =========================
# Why separate: needs transformers>=4.45, may conflict with coqui-tts 0.25.x
#
# IMPORTANT: Use Python 3.11 (or 3.10-3.13). Python 3.14 has no torch==2.6.0 wheels.
#
# Setup:
#   python3.11 -m venv .venv-csm
#   source .venv-csm/bin/activate
#   pip install -r requirements-csm.txt
#   huggingface-cli login   # free account + accept Meta Llama license
#
# Then run doctospeech from this venv.

epub2txt==0.1.6
docx2txt==0.9
pdftotext==4.0.0
html2text==2025.4.15
pyfiglet==1.0.4
tqdm==4.68.4
rich==15.0.0
ollama==0.6.2

# ========================= PDF OCR (scanned documents) =========================
pdf2image==1.17.0
pytesseract==0.3.13

torch==2.6.0
torchaudio==2.6.0
transformers==4.45.2
soundfile==0.14.0

# ========================= GRADIO WEB UI =========================
gradio>=5.0.0
